name: Sphinx Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Install pipenv dependencies
      - name: Install dependencies
        uses: VaultVulp/action-pipenv@v2.0.1
        with:
          command: install

      # Build sphinx source
      - name: Build sphinx site
        uses: VaultVulp/action-pipenv@v2.0.1
        with:
          command: run build

      # Commit built site to gh-pages branch.
      - name: Commit to github pages branch
        run: |
          git clone https://github.com/${{ github.repository }} --branch gh-pages --single-branch gh-pages
          rsync -av --delete docs/_build/ gh-pages
          cd gh-pages
          touch .nojekyll
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "Updated site." -a || true
          # The above command will fail if no changes were present, so we ignore
          # that.

      # Push new site build
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          branch: gh-pages
          directory: gh-pages
          github_token: ${{ secrets.GITHUB_TOKEN }}
